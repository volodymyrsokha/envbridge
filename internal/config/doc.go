// Package config loads, validates, and saves .envbridge.yaml (committed
// project config) and ~/.config/envbridge (user identity and settings), with
// git-style upward discovery from the working directory. Implemented in M3.
package config
